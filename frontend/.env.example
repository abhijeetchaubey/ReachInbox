# ReachInbox Frontend Environment Variables

# Google OAuth 2.0 Client ID (from Google Cloud Console -> APIs & Services -> Credentials)
# Ensure http://localhost:3000 is added to Authorized JavaScript origins
VITE_GOOGLE_CLIENT_ID=""
